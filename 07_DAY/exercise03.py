age = [22, 19, 24, 25, 26, 24, 25, 24]
length_list = len(age)
age = set(age)
length_set = len(age)
print(length_list, "=", length_set)

#The difference between string, list, tuple and set is that the string is a variable with diferent characters, the list is a group of strings that it could be modifies, the tuple is the same as the list but it cant be modified and finally the set is a collection of items with a random order.
